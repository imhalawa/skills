# skills

Personal Claude Code skills, shareable/reusable across machines.

## Layout

One folder per skill: `skills/<name>/SKILL.md`. Follows the same convention as other skill-source repos (e.g. `kepano/obsidian-skills`) so it works with the standard installer:

```
npx skills add imhalawa/skills
```
